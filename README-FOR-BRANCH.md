# Branch: `labs-deploy`

Denne branch'en eksisterer for å kunne automatisere deploy av `fyllut`-applikasjonen til labs.
Den skal aldri merges til master, men vil bli holdt oppdatert med nyeste versjoner av publiserte
skjemaer og oversettelser når deploy trigges.

Se workflow `trigger-labs-deploy.yaml`.
